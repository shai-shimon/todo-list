version: '3.3'
services:
  cache:
    container_name: "todo-redis"
    image: redis:6.2-alpine
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --appendonly yes
    volumes: 
      - cache:/data
volumes:
  cache:
    driver: local