import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            light: "#5CCAFF",
            main: "#5CCAFF",
            dark: "#424242",
            contrastText: "#000",
        },
        secondary:{
            main:'#424242s'
        }
    },
});